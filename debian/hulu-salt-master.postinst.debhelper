# Automatically added by dh_installinit
if [ -x "/etc/init.d/hulu-salt-master" ]; then
	if [ ! -e "/etc/init/hulu-salt-master.conf" ]; then
		update-rc.d hulu-salt-master defaults >/dev/null
	fi
	invoke-rc.d hulu-salt-master start || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
update-rc.d -f hulu-salt-master remove >/dev/null || exit $?
# End automatically added section
