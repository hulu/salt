# Automatically added by dh_installinit
if [ -x "/etc/init.d/hulu-salt-minion" ]; then
	if [ ! -e "/etc/init/hulu-salt-minion.conf" ]; then
		update-rc.d hulu-salt-minion defaults >/dev/null
	fi
	invoke-rc.d hulu-salt-minion start || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
update-rc.d -f hulu-salt-minion remove >/dev/null || exit $?
# End automatically added section
